//! Integration-test-only crate. See `tests/` for the end-to-end scenarios that
//! wire RiskAttestation + AttestorRegistry + WalletIdentity + MockLendingPool
//! together in a single Soroban test environment.
