# Soham Standup — 2026-07-03 (Day 7)

## Shipped today — lending demo complete + KYC badge pass

### Shared badge components (`components/Badges.tsx`)
Extracted three reusable badges to make the `kyc_verified` (and ZK / risk)
indicators consistent across the app:
- `ZkBadge({ verified })` — ZK-verified vs hash-anchored.
- `KycBadge()` — attestor-certified KYC.
- `RiskBadge({ bucket })` — colored dot + bucket label.

Applied them in `Wallet.tsx`, `Identity.tsx`, and `Lending.tsx` (removing the
duplicated inline badge markup that had drifted between pages).

### Lending demo (`Lending.tsx` + `mock-lending-pool.ts`)
- Added `executeLoan(wallet)` write client (`invokeContractCall` → `execute_loan`).
- `Lending.tsx` now supports the full borrow lifecycle:
  - Look up terms for **any** address (unchanged), **or** "Connect wallet" to
    fetch your own terms via Freighter.
  - Before/after terms comparison with the consistent KYC badge.
  - **Execute loan** — Freighter-signed `execute_loan`, shown only for the
    connected wallet's own fetched terms, with tx-hash / error feedback.
- Honest labeling kept: MockLendingPool is a demo (no real capital moves); the
  button exercises the risk-gated borrow flow end-to-end on testnet.

## Verification
```
frontend: npx tsc -b       # clean
frontend: npm run build    # clean (Node 20)
```
Connect → fetch → execute is validated manually in-browser (Freighter ext); the
`execute_loan` / `get_loan_terms` contract paths were confirmed live in the
Day-5 CLI smoke test.

## Blocked on
- Nothing.

## Planning next (Day 8 — DG6)
- circom Poseidon identity circuit: compile, snarkjs Groth16 setup, generate a
  proof, convert VK → binary blob, register via `register_verification_key`, and
  confirm `verify_groth16` accepts it on testnet.
- On success, swap the Identity SHA-256 stand-in commitment for Poseidon and add
  the `proof_bytes` param to `register_wallet`.
