# Soham Standup — 2026-07-01 (Day 5)

## Decision: struct reconciliation
The Day-4 notes floated removing `kyc_verified`/`identity_commitment` to match
`architecture.md`. Confirmed with the team to **keep the approved KYC +
multi-wallet plan** instead. Reconciled in the correct direction: updated
`architecture.md` to document the actual frozen struct (it now lists
`kyc_verified` + `identity_commitment`); `display_score` is deferred (frontend
derives a score from `risk_bucket` + `confidence` for now).

## Shipped today

### On-chain: multi-wallet group resolution (Option A)
- `RiskAttestation`: added `set_wallet_identity` (admin-only) + a local
  `WalletIdentityClient` cross-contract trait.
- `get_attestation` now resolves a wallet's `identity_commitment` to the shared
  group attestation when WalletIdentity is wired — any wallet in a group surfaces
  the group's best score, and the querying wallet's own record is hidden when a
  group score exists. Falls back to the individual record otherwise.

### Deploy + bindings
- `deploy-testnet.sh` now deploys **4 contracts** (added WalletIdentity) and wires
  `RiskAttestation::set_wallet_identity`. Writes `CONTRACT_ID_WALLET_IDENTITY` to
  `.env.local` and `VITE_CONTRACT_ID_WALLET_IDENTITY` to `frontend/.env.local`.
- `.env.example` / `frontend/.env.example` updated with the new var.
- `make bindings` regenerates all 4 TS packages; `set_wallet_identity` confirmed
  present in the risk-attestation binding.

### Frontend (Day 5 skeleton)
- `lib/contracts/wallet-identity.ts`: `getGroupAttestation(commitment)` read client.
- Extracted `parseAttestationData` (shared by risk-attestation + wallet-identity)
  and `bytes.ts` (`toHex`/`fromHex`).
- `config.ts`: added `walletIdentity` contract id.
- `Identity.tsx` (new) + `/identity` route + nav link: client-side identity-secret
  generation, commitment (SHA-256 stand-in until Poseidon/DG6), group-score lookup,
  KYC explainer. Wallet-link (register) step stubbed for Day 6 (needs proof + Freighter).

### Python helper fixes (was blocked)
- Fixed `hash_val()`: `scval.to_bytes(data)` (the old `to_xdr_bytes` does not exist).
- Fixed `identity_commitment` encoding: `Some` → inner bytes, `None` → `to_void`
  (the old `scval.to_some` does not exist in stellar-sdk 14.1.1).
- Offline SCVal smoke test: both None/Some cases build and round-trip to XDR;
  hash-length guard works.

## Verification (all green)
```
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace          # 8 passed (2 DG1, 2 lending, 4 wallet-identity)
make build-contracts            # 4 wasm
make bindings                   # 4 TS packages
frontend: npx tsc -b && npm run build   # Node 20, clean
python: offline SCVal construction smoke test
```

### Live testnet smoke test (redeployed 4-contract stack)
| Contract | Testnet ID |
|---|---|
| RiskAttestation | `CC5DE4MPF2P3LC42A2GQR73P34TNRTGNYCMTNFA6IJWSGPSM5MTFCLKE` |
| AttestorRegistry | `CCS2ZNRJA5RGBHJ4XDZPCFHPLQUEASBXZV7JFDH7SYOFLRYO37XEE3R5` |
| MockLendingPool | `CABFLZP6ZECO4CADMF7BGBP4LT7CKYJR26GBSTGVFGCVD6NWJDYUCGD7` |
| WalletIdentity | `CDY6JAQJ5GZJDOZIP4I65T4FUAWT25YEW65X7MDOZKA2LFC2AWOEWQ26` |

- `get_attestation` (no record) → `null` ✓
- `get_loan_terms` → default terms (MockLendingPool → RiskAttestation cross-call live) ✓
- `get_group_attestation` (no group) → `null` ✓
- `register_wallet` → success ✓
- `register_wallet` (duplicate) → `Error(Contract, #12)` = `AlreadyInGroup` ✓

## Blocked on
- Nothing.

## Planning next (Day 6)
- `Identity.tsx` complete: Freighter-signed `register_wallet`, group score display end-to-end.
- Poseidon commitment (replaces SHA-256 stand-in) once DG6 circuit lands (Day 8).
- Optional: full attest → register → group-score E2E via Python helper / frontend.
