# Soham Standup — 2026-07-02 (Day 6)

## Shipped today — Identity.tsx complete (Freighter-signed end-to-end)

### Freighter integration (`lib/freighter.ts`)
Replaced the non-functional `useFreighter` stub with working helpers:
- `connectFreighter()` — `isConnected` guard + `requestAccess`, returns the public key.
- `getConnectedAddress()` — non-prompting current address.
- `signWithFreighter(xdr, passphrase, address)` — wraps `signTransaction`.

### Write-transaction path (`lib/contracts/rpc.ts`)
Added `invokeContractCall(contractId, method, args, sourceAddress)`:
build → `prepareTransaction` (simulate + assemble footprint/resource fee/auth)
→ Freighter sign → `sendTransaction` → poll `getTransaction` to SUCCESS.
The connected wallet is the tx source, so its signature satisfies the contract's
`require_auth()`. Returns the tx hash; throws on submit/on-chain failure.

### Write clients (`lib/contracts/wallet-identity.ts`)
- `registerWallet(wallet, commitment)` → `register_wallet` (matches the deployed
  signature: `(wallet, commitment)`, no proof param yet).
- `leaveGroup(wallet)` → `leave_group`.
- Extracted `commitmentScVal()` (32-byte hex → `BytesN<32>` ScVal), shared with
  the existing `getGroupAttestation` read.

### Identity.tsx
- **Step 1** — generate identity secret (32 bytes, Web Crypto), SHA-256 stand-in
  commitment (Poseidon at DG6), download backup.
- **Step 2** — "Connect Freighter" → shows connected address → "Link wallet"
  (`register_wallet`, Freighter-signed) with tx-hash / error feedback, plus a
  "Leave group" action. Replaces the Day-5 disabled stub.
- **Step 3** — group-score lookup by commitment (unchanged).
- **Step 4** — KYC explainer.

## Verification
```
frontend: npx tsc -b            # clean
frontend: npm run build         # clean (Node 20)
```
End-to-end signing (connect → link → group score) requires the Freighter browser
extension, so it is validated manually in-browser; the deployed
`register_wallet` write path was already confirmed live via the Day-5 CLI smoke
test (success + duplicate → Error #12 AlreadyInGroup).

## Notes / next
- Commitment remains a SHA-256 stand-in; swap to Poseidon once the DG6 circuit
  lands (Day 8), at which point linking will also carry a ZK proof of secret
  knowledge (contract gains a `proof_bytes` param + `verify_groth16` check).
- Day 7 (Soham): lending demo polish + `kyc_verified` badge pass across pages.

## Blocked on
- Nothing.
