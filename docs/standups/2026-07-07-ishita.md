# Ishita Standup — 2026-07-07

## Frontend deployment and attestation flow hardening

Today focused on the deployed frontend path and the transition away from the
old local/demo assumptions.

### Shipped today

- **Documented the remaining work clearly** in:
  - `docs/remaining-work-audit.md`
  - `docs/v2_plan.md`
- **Verified the real deployed surfaces**:
  - Vercel frontend live at `https://zkredit-app.vercel.app`
  - Fly backend live at `https://zkredit-api.fly.dev`
  - FastAPI docs/openapi reachable on Fly
- **Confirmed and diagnosed the deployed browser failure**:
  - proved the issue was backend CORS, not Vercel env wiring
  - confirmed the Vercel build embeds the Fly backend URL
  - handed off the exact CORS fix requirement to Soham
- **Unified frontend backend targeting**
  - frontend now uses `VITE_API_URL` as the canonical deployed API env var
  - removed the old `VITE_API_BASE_URL` fallback from the active frontend API
    client path
- **Improved attestation error handling**
  - classified backend/API preparation failures into clearer kinds:
    - backend unavailable
    - session failed
    - rate limited
    - already attested
    - live proof preparation unavailable
  - replaced the raw backend error surface in `OnChainAttest` with clearer
    user-facing messages
- **Started the next frontend phase**
  - refactored `OnChainAttest` toward explicit process phases instead of one
    broad "attesting" state:
    - creating session
    - preparing
    - waiting / queued for proving
    - signing
    - submitting
    - reading
- **Made the frontend forward-compatible with async proving jobs**
  - `prepareAttestation()` now accepts either:
    - immediate `partial_xdr`
    - queued `{ job_id, status }`
  - added a real frontend client for:
    - `GET /api/v1/attest/jobs/{job_id}`
  - `OnChainAttest` now polls queued jobs and automatically resumes the
    signing/submission flow if the backend later returns a completed
    co-signable payload
  - if the backend still lacks the job-status API, the UI now fails honestly
    with a specific queued-proving message instead of stalling silently
- **Added frontend coverage for the queued-job client path**
  - attestor client tests now cover:
    - queued job status fetch
    - missing backend job-status support mapping
- **Hardened frontend quality gates**
  - added a real `npm run lint` script in `frontend/package.json`
  - added the missing ESLint dev dependencies to the frontend manifest
  - added `frontend/tsconfig.eslint.json`
  - tightened `frontend/eslint.config.mjs` so lint covers the maintained app
    surface instead of stale exploratory pages/config files
  - verified:
    - `npm run lint`
    - `npm run test`
    - `npm run typecheck`
    - `npm run build`
- **Reduced landing-page initial bundle pressure**
  - lazy-loaded the attestation route from `App.tsx`
  - removed the eager Freighter import path from the shared nav button
  - split motion / Freighter / Stellar chunks in Vite output
  - result: the main landing bundle dropped from roughly `1.4 MB` minified to
    about `224 KB` minified, with the heavier Stellar code moved off the
    initial landing-page path
- **Finished the remaining wallet/network hardening pass**
  - added real Freighter network checking against Stellar Testnet
  - surfaced a specific wrong-network message instead of a generic failure
  - classified chain submit problems into clearer contract/RPC error kinds:
    - wallet/account unavailable or underfunded
    - submit timeout
    - on-chain failure
  - routed those messages through `OnChainAttest`
- **Pushed the attestation route bundle split further**
  - moved contract-heavy imports inside the attestation action path
  - separated `ContractRpcError` into its own light module so the UI can import
    the error type without pulling the Stellar SDK immediately
  - result: the attestation route code stays lighter until the wallet action
    actually needs the Soroban/Stellar client
- **Added component-level tests for attestation UI states**
  - `OnChainAttest` now has direct UI tests covering:
    - wrong Freighter network
    - rate-limited preparation
    - queued proving state rendering

### Verified

- `npm run typecheck`
- `npm run build`
- `npm run lint`
- `npm run test`

All four pass after the frontend changes.

### Could not verify locally

- backend pytest still does not run in this workspace because the local Python
  environment here is missing backend dependencies such as `numpy`

### Blocked on / external dependency

- **Fly backend proving environment is still unavailable** for live co-sign
  preparation. The frontend now reaches the backend successfully, but the
  backend returns:
  - `RISC Zero co-sign preparation is unavailable in this environment`
- This is a backend/prover/runtime problem, not a frontend routing problem.
- **CORS issue was fixed server-side** after handoff; browser preflight now
  succeeds for `https://zkredit-app.vercel.app`.
- **Async proving job route still needs the real backend implementation**
  - frontend is ready to poll `GET /api/v1/attest/jobs/{job_id}`
  - backend route exists in repo now, but intentionally returns `501` until the
    proving-job store and worker are implemented and deployed

### Next

- Backend needs to actually expose `GET /api/v1/attest/jobs/{job_id}` before
  the queued proving path can work end-to-end.
- optional frontend follow-up: further split the large Stellar SDK chunk if you
  want the attestation route itself to load even lighter before wallet actions
